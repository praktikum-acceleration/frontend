export default function({ store, redirect }) {
  // if(!store.state.user) return redirect('/?message=noUser')
  // if(!Object.keys(store.state.user).length) {
  //   redirect('/?message=noUser')
  // }
}
